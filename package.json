{
  "name": "gdgoc-gcee",
  "version": "1.0.0",
  "private": true,
  "description": "GDGoC on Campus — Government College of Engineering, Erode. Full-stack community platform (React + Express + MongoDB).",
  "scripts": {
    "install:all": "npm install --prefix backend && npm install --prefix frontend",
    "build": "npm run build --prefix frontend",
    "dev:backend": "npm run dev --prefix backend",
    "dev:frontend": "npm run dev --prefix frontend",
    "seed": "npm run seed --prefix backend"
  },
  "engines": {
    "node": ">=20.0.0"
  },
  "dependencies": {
    "nodemailer": "^9.0.5"
  },
  "devDependencies": {
    "concurrently": "^10.0.5"
  }
}
